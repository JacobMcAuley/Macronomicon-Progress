import * as Blockly from 'blockly';
import { BlockTypes, BlockNames } from '../block-definitions';

// Blockly.Extensions.register(`${BlockNames.LogicCompare}`, function (this: Blockly.Block) {
//     this.appendValueInput(new Blockly.JavaScript.).setCheck('Number');
//     this.setOutput(true, BlockTypes.Operator);
// });

// console.log('y');
